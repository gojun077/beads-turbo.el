db: ""
no-db: false

# dolthub remote (not git+ssh remote)
sync:
    mode: dolt-native
sync-branch: beads-sync
sync.remote: "gojun077/beads_bdel"

# JSONL backup — disabled; Dolt + DoltHub is the source of truth
backup:
  enabled: false

# Disable auto-export of issues.jsonl
export.auto: false
import.auto: false

dolt.shared-server: true
